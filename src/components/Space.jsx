import React from 'react';
const space = ({planet,cost}) => (
    <div>
        I want to travel to {planet}. It only costs {cost}
        <a href=''>Övningar</a>
    </div>
    
    )
export default space;
